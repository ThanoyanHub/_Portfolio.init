Install Node.js (if you haven't).

Put all files in one folder.

Open your terminal in that folder.

Run ``` npm install express ```to get the server dependency.

Run 
```node server.js```

Open` http://localhost:3000 `in your browser.
